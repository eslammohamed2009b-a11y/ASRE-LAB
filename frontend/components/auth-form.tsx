"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {getSupabase} from "@/lib/supabase";
export function AuthForm({mode}:{mode:"sign-up"|"log-in"}){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false),router=useRouter(),signup=mode==="sign-up";
async function submit(e:FormEvent){e.preventDefault();setMessage("");if(password.length<8){setMessage("Use at least 8 characters.");return}setBusy(true);try{const s=getSupabase();if(signup){const{data,error}=await s.auth.signUp({email,password});if(error)throw error;if(!data.session){setMessage("Check your email to confirm your account, then log in.");return}}else{const{error}=await s.auth.signInWithPassword({email,password});if(error)throw error}const requested=new URLSearchParams(window.location.search).get("returnTo");router.replace(requested?.startsWith("/app/")&&!requested.startsWith("//")?requested:"/app/dashboard")}catch(error){setMessage(error instanceof Error?error.message:"Authentication failed. Check your network and try again.")}finally{setBusy(false)}}
return <form onSubmit={submit} noValidate><div className="field"><label htmlFor="email">Email address</label><input id="email" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label htmlFor="password">Password</label><input id="password" type="password" minLength={8} autoComplete={signup?"new-password":"current-password"} required value={password} onChange={e=>setPassword(e.target.value)}/><small className="muted">{signup?"At least 8 characters.":"Use the password for your ASRE–Lab account."}</small></div>{message&&<p className={message.startsWith("Check")?"info":"error"} role="alert">{message}</p>}<div className="form-actions"><button disabled={busy}>{busy?(signup?"Creating account…":"Logging in…"):(signup?"Create account":"Log in")}</button></div><p className="muted">{signup?"Already have an account? ":"Need an account? "}<Link href={signup?"/auth/log-in":"/auth/sign-up"}>{signup?"Log in":"Create one"}</Link></p></form>}
