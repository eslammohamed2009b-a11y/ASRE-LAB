import {getSupabase} from "./supabase";
export class ApiError extends Error{constructor(public status:number,message:string,public code?:string){super(message)}}
function proxyPath(path:string){return `/_asre-api${path.startsWith("/")?path:`/${path}`}`}
export async function api<T>(path:string,init:RequestInit&{idempotencyKey?:string}={}):Promise<T>{const {data}=await getSupabase().auth.getSession();if(!data.session)throw new ApiError(401,"Your session has expired.");const headers=new Headers(init.headers);headers.set("Authorization",`Bearer ${data.session.access_token}`);if(init.body&&!headers.has("Content-Type"))headers.set("Content-Type","application/json");if(init.idempotencyKey)headers.set("Idempotency-Key",init.idempotencyKey);const response=await fetch(proxyPath(path),{...init,headers});if(response.status===401)await getSupabase().auth.signOut();if(!response.ok){const body=await response.json().catch(()=>({}));throw new ApiError(response.status,body.detail||"The request could not be completed.",body.code)}return response.json() as Promise<T>}
export async function download(path:string){const {data}=await getSupabase().auth.getSession();if(!data.session)throw new ApiError(401,"Sign in to download this artifact.");const response=await fetch(proxyPath(path),{headers:{Authorization:`Bearer ${data.session.access_token}`}});if(!response.ok)throw new ApiError(response.status,"The artifact is unavailable.");return{blob:await response.blob(),disposition:response.headers.get("content-disposition")}}
